---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: novanetworkpolicies.novanet.io
spec:
  group: novanet.io
  names:
    kind: NovaNetworkPolicy
    listKind: NovaNetworkPolicyList
    plural: novanetworkpolicies
    shortNames:
    - nnp
    singular: novanetworkpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NovaNetworkPolicy is the Schema for the novanetworkpolicies API.
          It extends Kubernetes NetworkPolicy with port ranges, FQDN-based peers,
          and service account selectors.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NovaNetworkPolicySpec defines the desired state of NovaNetworkPolicy.
            properties:
              egress:
                description: |-
                  Egress specifies egress rules. Each rule allows traffic to destinations
                  matching To on the specified Ports.
                items:
                  description: NovaNetworkPolicyEgressRule describes an egress rule.
                  properties:
                    ports:
                      description: Ports specifies the ports and protocols allowed.
                      items:
                        description: NovaNetworkPolicyPort describes a port with optional
                          range support.
                        properties:
                          endPort:
                            description: |-
                              EndPort defines the end of a port range (inclusive). If set, Port must
                              also be set and EndPort must be >= Port.
                            format: int32
                            type: integer
                          port:
                            description: Port is the port number.
                            format: int32
                            type: integer
                          protocol:
                            description: Protocol is the protocol (TCP, UDP, SCTP).
                              Defaults to TCP.
                            type: string
                        type: object
                      type: array
                    to:
                      description: To specifies destinations the selected pods are
                        allowed to reach.
                      items:
                        description: NovaNetworkPolicyPeer describes a peer for policy
                          rules.
                        properties:
                          fqdn:
                            description: |-
                              FQDN specifies a fully qualified domain name. The controller resolves
                              it to IP addresses and generates CIDR rules for each resolved address.
                            type: string
                          ipBlock:
                            description: IPBlock specifies a CIDR range and optional
                              exceptions.
                            properties:
                              cidr:
                                description: CIDR is the IP block (e.g. "10.0.0.0/8").
                                type: string
                              except:
                                description: Except are CIDRs that should not be included
                                  within the CIDR.
                                items:
                                  type: string
                                type: array
                            required:
                            - cidr
                            type: object
                          namespaceSelector:
                            description: |-
                              NamespaceSelector selects namespaces. Combined with PodSelector, this
                              selects pods in the matching namespaces.
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector
                                  requirements. The requirements are ANDed.
                                items:
                                  description: |-
                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                    relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values. If the operator is In or NotIn,
                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced during a strategic
                                        merge patch.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                            x-kubernetes-map-type: atomic
                          podSelector:
                            description: |-
                              PodSelector selects pods in the policy's namespace (or in namespaces
                              selected by NamespaceSelector).
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector
                                  requirements. The requirements are ANDed.
                                items:
                                  description: |-
                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                    relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values. If the operator is In or NotIn,
                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced during a strategic
                                        merge patch.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                            x-kubernetes-map-type: atomic
                          serviceAccount:
                            description: ServiceAccount selects pods by their service
                              account identity.
                            properties:
                              name:
                                description: Name is the service account name.
                                type: string
                              namespace:
                                description: |-
                                  Namespace is the namespace of the service account. If empty, the policy's
                                  namespace is used.
                                type: string
                            required:
                            - name
                            type: object
                        type: object
                      type: array
                  type: object
                type: array
              ingress:
                description: |-
                  Ingress specifies ingress rules. Each rule allows traffic from sources
                  matching From on the specified Ports.
                items:
                  description: NovaNetworkPolicyIngressRule describes an ingress rule.
                  properties:
                    from:
                      description: From specifies sources allowed to reach the selected
                        pods.
                      items:
                        description: NovaNetworkPolicyPeer describes a peer for policy
                          rules.
                        properties:
                          fqdn:
                            description: |-
                              FQDN specifies a fully qualified domain name. The controller resolves
                              it to IP addresses and generates CIDR rules for each resolved address.
                            type: string
                          ipBlock:
                            description: IPBlock specifies a CIDR range and optional
                              exceptions.
                            properties:
                              cidr:
                                description: CIDR is the IP block (e.g. "10.0.0.0/8").
                                type: string
                              except:
                                description: Except are CIDRs that should not be included
                                  within the CIDR.
                                items:
                                  type: string
                                type: array
                            required:
                            - cidr
                            type: object
                          namespaceSelector:
                            description: |-
                              NamespaceSelector selects namespaces. Combined with PodSelector, this
                              selects pods in the matching namespaces.
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector
                                  requirements. The requirements are ANDed.
                                items:
                                  description: |-
                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                    relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values. If the operator is In or NotIn,
                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced during a strategic
                                        merge patch.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                            x-kubernetes-map-type: atomic
                          podSelector:
                            description: |-
                              PodSelector selects pods in the policy's namespace (or in namespaces
                              selected by NamespaceSelector).
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector
                                  requirements. The requirements are ANDed.
                                items:
                                  description: |-
                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                    relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values. If the operator is In or NotIn,
                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced during a strategic
                                        merge patch.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                            x-kubernetes-map-type: atomic
                          serviceAccount:
                            description: ServiceAccount selects pods by their service
                              account identity.
                            properties:
                              name:
                                description: Name is the service account name.
                                type: string
                              namespace:
                                description: |-
                                  Namespace is the namespace of the service account. If empty, the policy's
                                  namespace is used.
                                type: string
                            required:
                            - name
                            type: object
                        type: object
                      type: array
                    ports:
                      description: Ports specifies the ports and protocols allowed.
                      items:
                        description: NovaNetworkPolicyPort describes a port with optional
                          range support.
                        properties:
                          endPort:
                            description: |-
                              EndPort defines the end of a port range (inclusive). If set, Port must
                              also be set and EndPort must be >= Port.
                            format: int32
                            type: integer
                          port:
                            description: Port is the port number.
                            format: int32
                            type: integer
                          protocol:
                            description: Protocol is the protocol (TCP, UDP, SCTP).
                              Defaults to TCP.
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
              podSelector:
                description: PodSelector selects the pods to which this policy applies.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              policyTypes:
                description: PolicyTypes specifies which rule types (Ingress, Egress)
                  this policy includes.
                items:
                  description: PolicyType describes the direction of traffic a policy
                    applies to.
                  enum:
                  - Ingress
                  - Egress
                  type: string
                type: array
            required:
            - podSelector
            type: object
          status:
            description: NovaNetworkPolicyStatus defines the observed state of NovaNetworkPolicy.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the policy's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              ruleCount:
                description: RuleCount is the number of compiled rules generated from
                  this policy.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
